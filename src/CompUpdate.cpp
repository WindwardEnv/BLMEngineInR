#include <Rcpp.h>
using namespace Rcpp;

std::vector<double> CompUpdate(int x) {
  std::vector<double> d;
  return d;
  // // arma::vec d;
  // // arma::mat U;
  // // arma::mat V;
  // //
  // // arma::svds(U,d,V,X);
  // //
  // //return d;
  //
  // Function Rsvd("base::svd");
  //
  // arma::mat X(5, 5);
  // for (unsigned int i = 0; i < 5; i++){
  //   for (unsigned int j = 0; j < 5; j++){
  //     X[i, j] = i + j;
  //   }
  // }
  //
  // arma::mat U;
  // arma::vec s;
  // arma::mat V;
  //
  // svd(U,s,V,X);
  //
  // return s;
}
